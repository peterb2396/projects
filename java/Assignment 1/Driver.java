package assignment1;

import java.io.FileNotFoundException;

/**
 * Tests the design of ExpressionTree
 * @author Peter Buonaiuto
 * @version 1.0
 */
public class Driver {
	/**
	 * Invokes the methods of Helper to test the design of ExpressionTree
	 * @param args command-line arguments
	 * @throws FileNotFoundException if can not find proper file
	 */
	public static void main(String[] args) throws FileNotFoundException {
		 Helper.start();
		}
	}
