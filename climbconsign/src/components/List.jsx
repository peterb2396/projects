//import React, {useState} from "react";

const List = props => {
    // Return a list of all matching sellers

    // we have props.search, which is the keyword by user
    // put this in a axios request to the new endpoint FindCustomer and pass 'search' to filter
    // render each result as a memberComponent (memberListing)
    //const [memberComps, setMemberComps] = useState([])


    return (
        <div id = "memberResults">
        </div>
    )
}

export default List;